# Pre-commit hook for Git hooks setup
# This hook runs before each commit to ensure code quality

# Run lint-staged on staged files
echo "Running lint-staged..."
npx lint-staged

# If all checks pass, the commit will proceed
